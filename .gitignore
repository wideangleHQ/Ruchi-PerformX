# Agent working directories. client/, server/, and docs/ have their own ignores.
.claude/
.agents/

# Regenerated by `graphify update`. One AST file per source file, and
# .graphify_root holds an absolute path, so neither belongs in a diff.
graphify-out/cache/
graphify-out/graph.html
graphify-out/.graphify_root
